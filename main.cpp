#include <iostream>
#include <sum.h>
#include <dif.h>
using namespace std;
void main() {
     cout <<" Hello, GIT" << endl;
	 int a, b;
	 cout << "Enter two numbers: ";
	 cin >> a >> b;
	 Sum(a,b)
	 dif(a,b)
	
}